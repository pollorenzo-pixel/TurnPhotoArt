# Phase 1 requires no environment variables or API keys.
# The preserved legacy generation code is disabled at its route boundary.

# Optional, non-sensitive public deployment URL. Omit for local development.
# SITE_URL=https://turnphotoart.example

# Indexing remains disabled unless this is true AND SITE_URL is valid.
# SITE_INDEXING_ENABLED=false
